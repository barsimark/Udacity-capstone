{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "create-post-request",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "minLength": 3
        }
    },
    "required": ["name"],
    "additionalProperties": {
        "properties": {
            "attachmentUrl": {
                "type": "string"
            }
        }
    }
}